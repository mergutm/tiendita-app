# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependency directories & files
# ------------------------------------------------------------------------------
/node_modules
/.pnp      # Yarn Plug'n'Play directory (if applicable)
.pnp.* # Yarn Plug'n'Play files (like .pnp.cjs, .pnp.loader.mjs)
/.yarn/* # Yarn Berry directory structure
# Exceptions for Yarn Berry Zero Installs (if using, keep these lines)
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build output, cache & logs
# ------------------------------------------------------------------------------
/build
/coverage
/.next/
/.vscode/
/out/
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# Consider adding general log patterns if your app creates them, e.g., logs/, *.log

# Generated files (specific to Next.js/TypeScript)
# ------------------------------------------------------------------------------
next-env.d.ts # Auto-generated by Next.js for TypeScript environment types

# Local environment variables
# ------------------------------------------------------------------------------
# Never commit local overrides or secrets.
# It's recommended to commit example files (e.g., .env.example)
.env*.local
# Usually, the main .env file should also be ignored if it contains secrets.
# If your .env file is just for non-secret default values and meant to be committed, remove the line below.
.env

# Vercel deployment configuration
# ------------------------------------------------------------------------------
.vercel

# OS-specific files
# ------------------------------------------------------------------------------
.DS_Store         # macOS
Thumbs.db         # Windows

# Security sensitive files (examples)
# ------------------------------------------------------------------------------
# Add any other sensitive file types you might use
*.pem
*.key